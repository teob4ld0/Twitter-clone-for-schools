# Vite only exposes env vars prefixed with VITE_
# Create a .env.local (ignored by git) based on this file.

# Google OAuth Client ID (Web application) from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=

# API URLs (for production overrides if needed)
# VITE_API_URL=https://io.twittetec.com
# VITE_APP_URL=https://app.twittetec.com
